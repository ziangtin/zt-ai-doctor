{
  "name": "catalog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/build-data.mjs",
    "build": "vitepress build",
    "dev": "node scripts/build-data.mjs && vitepress dev",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "vue": "^3.5.40"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^14.0.0",
    "vitepress": "^1.6.4"
  }
}
